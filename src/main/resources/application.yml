logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%X{traceId}] [%thread] %-5level %logger{36} - %msg%n"
spring:
  application:
    name:demo-exchange
  threads:
    virtual:
      enabled: true

  sql:
    init:
      mode: always
  datasource:
    url: jdbc:h2:mem:testdb